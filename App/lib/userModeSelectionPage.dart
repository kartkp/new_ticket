// Import Flutter plugin
import 'package:flutter/material.dart';

class userModeSelectionPage extends StatelessWidget {
  const userModeSelectionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Column(

      ),
    );
  }
}
