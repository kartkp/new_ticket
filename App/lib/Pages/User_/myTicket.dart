import 'package:flutter/material.dart';

class myTicket extends StatelessWidget {
  const myTicket({super.key});

  @override
  Widget build(BuildContext context) {
    return const
    Center(child: Text("This is My Ticket Section"));

  }
}